!!!
%html
  %head
    %title TwitterNewspaper
    = stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true
    = javascript_include_tag 'application', 'data-turbolinks-track' => true
    = csrf_meta_tags
  %body
      

    %header.navbar.navbar-fixed-top.navbar-inverse
      .navbar-inner
        .container
          = image_tag("rails.jpg", alt: "Rails", size: "70x70")
          = link_to "Twitter Newspaper", '/', id: "logo"
          1
          %nav
            %ul.nav.pull-right              
              -if user_signed_in?
                %font{:color => "steelblue"}
                  Signed in as #{current_user.email}. Not you?
                = link_to "Sign out", destroy_user_session_path, :method => :delete, class: "btn btn-medium btn-primary"
              -else
                = link_to "Sign in", new_user_session_path, class: "btn btn-medium btn-primary"
                = link_to "Sign up", new_user_registration_path, class: "btn btn-medium btn-primary"
                

    %footer
      #footer
        %font{:color => "white",}
          %h5{:align => "center"}
            Current date: #{Time.now}          
    .container
      = yield
